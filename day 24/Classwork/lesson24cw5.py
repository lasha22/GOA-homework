def to_jaden_case(string):
    words = string.split()
    jaden_cased_words = []

    for words in words:
        jaden_cased_words = words.capitalize()
        jaden_cased_words.append(jaden_cased_words)
    
    return ' '.join(jaden_cased_words)
